export enum RoleType {
  Admin = 'Admin',
  User = 'User',
}

export enum RoleTypeIndex {
  Admin = 1,
  User = 3,
}

export enum PaymentType {
  Payze = 1,
}

export enum PaymentStatusType {
  Pending = 1,
  Success,
  Fail,
}
